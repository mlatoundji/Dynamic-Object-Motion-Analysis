# Rapport d’entraînement — CNN‑LSTM (IPN Hand)

Ce fichier est **généré** à partir d’un dossier de run (`runs/...`) via:

```bash
poetry run doma-train report --run runs/<run_name> --out docs/REPORT_CNN_LSTM.md
```

Si vous lisez ce fichier avant génération, lancez une première fois un entraînement puis la commande ci‑dessus.

